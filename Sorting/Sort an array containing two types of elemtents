public static void sortArrayWith2Elements(int arr[])
	{
		// assuming that array contains negative and positive elements..
		
		int res = 0;
		for(int i = 0 ; i < arr.length ; i++)
		{
			if(arr[i] < 0)
			{
				swap(arr , res++, i);
			}	
		}
		System.out.println(Arrays.toString(arr));
	}
